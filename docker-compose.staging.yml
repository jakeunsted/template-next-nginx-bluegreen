
version: '3.8'
services:
  app-blue:
    build:
      context: ./app
      dockerfile: Dockerfile
      target: production
    image: app-staging-blue
    container_name: app-staging-blue
    restart: always
    ports:
      - "3001:3000"

  app-green:
    build:
      context: ./app
      dockerfile: Dockerfile
      target: production
    image: app-staging-green
    container_name: app-staging-green
    restart: always
    ports:
      - "3002:3000"

  nginx:
    image: nginx:latest
    container_name: nginx-staging
    restart: always
    ports:
      - "80:80"
    volumes:
      - ./nginx/staging.conf:/etc/nginx/conf.d/default.conf
